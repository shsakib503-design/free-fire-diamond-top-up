rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /orders/{docId} {
      allow create: if true;         // public can submit form
      allow read: if request.auth != null; // only admin who is logged in
      allow update, delete: if false;
    }
  }
}
